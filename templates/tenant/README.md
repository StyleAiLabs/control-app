# Tenant Template

This directory is copied into each generated tenant runtime folder.

Provisioning then writes tenant-specific `.env` and `metadata.json` files and ensures the `config`, `data`, and `logs` folders exist.
